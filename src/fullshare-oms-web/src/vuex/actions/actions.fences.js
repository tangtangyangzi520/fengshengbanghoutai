import * as types from '../types'
import * as fencesTypes from '../types/types.fences'
import client from '../../common/utils/client';

// 更新栏位信息
export const updateFenceInfo = ({ dispatch }, data) => {;
        dispatch('updateFenceInfo', {})
    }
    // 创建的栏位本地信息
export const createFenceInfo = ({ dispatch }, data) => {;
    dispatch('createFenceInfo', data)
}